import { Route, Routes } from 'react-router';

export const SettingsRoutes = () => {
    return (
        <Routes>
            <Route path="/" element={<div>Hello</div>} />
        </Routes>
    );
};
